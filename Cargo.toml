[package]
name = "vex-sdk-sim"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0.82"
bitflags = "2.5.0"
bytemuck = "1.15.0"
bytes = "1.6.0"
fimg = { version = "0.4.41", features = ["save", "text"], default-features = false }
fontdue = "0.8.0"
png = "0.17.13"
resource = "0.5.0"
sdl2 = "0.36.0"
wasmparser = "0.204.0"
wasmtime = { version = "19.0.2", default-features = false, features = [
    # "cache",
    "parallel-compilation",
    "cranelift",
    # "pooling-allocator",
    "demangle",
    "addr2line",
    "coredump",
    "debug-builtins",
    "runtime",
] }
